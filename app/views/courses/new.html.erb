<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :site %><br>
    <%= f.select(:site, options_for_select([ 
                                        ['Treehouse', 'Treehouse'], 
                                        ['CodeSchool', 'Codeschool'] ])) %>
  </div>
  <div class="field">
    <%= f.label "Course Name" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Completion Date" %><br>
    <%= f.date_field :completion_date %>
  </div>
  <div class="field">
    <%= f.label :badge %><br>
    <%= f.text_area :badge %>
  </div>
  <div class="field">
    <%= f.label :hours %><br>
    <%= f.number_field :hours %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
